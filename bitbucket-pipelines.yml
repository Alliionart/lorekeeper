image: atlassian/default-image:latest

pipelines:
  default:
    - step:
        deployment: staging
        script:
          - ssh -o PasswordAuthentication=no root@46.224.150.124 'sh ./deploy.sh'
          - echo "Deploy step finished"
